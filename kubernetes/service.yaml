apiVersion: v1
kind: Service
metadata:
  name: service
  namespace: helenanogueira
spec:
  selector:
    app: recomendador
  type: NodePort
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
      nodePort: 28025